# Spot Render IA Auto Agent - Kubernetes Overlays

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: spot-render-ai-agents

resources:
  - ../base/k8s-agents.yaml

commonLabels:
  app.kubernetes.io/managed-by: kustomize
  app.kubernetes.io/instance: spot-render-ia-agents
